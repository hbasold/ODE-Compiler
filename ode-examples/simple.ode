
system {
    var five = 5;
    var three = 3;
    var x = integ(five * x - three, 1);
    emit x as x;
    interval five = [5, 5];
    interval three = [3, 3];
    interval x = [-60, 60];
    time 1;
}
